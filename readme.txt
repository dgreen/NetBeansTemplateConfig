The main class to be ran is titled "Main"
This is where the user inputs their information and selects which course they are in 
	(this option is not yet implemented)

If the user clicks the "generate templates" button before typing something in each text box, a new window will pop up, which prompts the user to "please fill in all fields"

The program pulls the templates files in the resources directory, and saves them as .java files into the proper folder based on the operating system and then exits