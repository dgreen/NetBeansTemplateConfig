/*
 * Author: ${user}
 * Assignment:  ${project.displayName} - ${course}
 *
 * Credits:  (if any for sections of code)
 */
<#if package?? && package != "">
package ${package};
</#if>

/**
 *
 * @author ${user}
 */
public interface ${name} {

}
